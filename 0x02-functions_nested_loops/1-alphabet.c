#include "main.h"
/**
 *	print_alphabet - write a function that prints lowercase _putchar
 *
 *
 *
 *	Return: 0
*/
void print_alphabet(void)
{
	char ch;

	if (ch >= 'a' && ch <= 'z')
	{
		_putchar(ch);
	}
	return (0);

}
